function character(word) {

    // Обхождаме всеки символ в текста
    for (let i = 0; i < word.length; i++) {

        // Отпечатваме текущия символ
        console.log(word[i]);
    }
}
character("softuni");